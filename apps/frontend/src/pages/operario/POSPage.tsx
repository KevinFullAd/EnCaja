// src/pages/operario/POSPage.tsx
import React from "react"
import { useAuth } from "@/app/providers/AuthProviders"

export default function POSPage() {
    const { user, logout } = useAuth()

    return (
        <div className="h-screen p-6">
            
        </div>
    )
}
